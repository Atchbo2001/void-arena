name: Export fix source

on:
  push:
    branches:
      - agent/fix-native-custom-games
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v4
        with:
          lfs: true
      - name: Create source archive
        run: |
          mkdir -p .tmp
          tar --exclude=.git --exclude=.tmp --exclude=.github/workflows/export-fix-source.yml -czf .tmp/void-arena-fix-source.tar.gz .
      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: void-arena-fix-source-${{ github.sha }}
          path: .tmp/void-arena-fix-source.tar.gz
          retention-days: 1
